{
  "name": "diagnosticos-mock-backend",
  "version": "1.0.0",
  "description": "Mock backend for diagnosticos_app Flutter study app",
  "private": true,
  "scripts": {
    "start": "json-server --watch db.json --routes routes.json --host 0.0.0.0 --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
